<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewModel"
            type="com.shv.android.composition.presentation.GameViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/tvTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{viewModel.formattedTime}"
            android:textColor="@color/purple_700"
            android:textSize="24sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="00:00" />

        <TextView
            android:id="@+id/tvSum"
            style="@style/QuestionTextStyle"
            android:layout_width="144dp"
            android:layout_height="144dp"
            android:background="@drawable/circle_background"
            app:layout_constraintBottom_toTopOf="@+id/tvVisibleNumber"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed"
            app:numberAsText="@{viewModel.question.sum}"
            tools:text="1" />

        <TextView
            android:id="@+id/tvVisibleNumber"
            style="@style/QuestionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/teal_700"
            app:layout_constraintBottom_toTopOf="@+id/tvOption1"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toStartOf="@+id/tvInvisibleNumber"
            app:layout_constraintHorizontal_chainStyle="packed"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tvSum"
            app:numberAsText="@{viewModel.question.visibleNumber}"
            tools:text="7" />

        <TextView
            android:id="@+id/tvInvisibleNumber"
            style="@style/QuestionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@color/teal_700"
            android:text="@string/question_mark"
            app:layout_constraintBottom_toBottomOf="@+id/tvVisibleNumber"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/tvVisibleNumber"
            app:layout_constraintTop_toTopOf="@+id/tvVisibleNumber" />

        <TextView
            android:id="@+id/tvOption1"
            style="@style/OptionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_green_light"
            android:text="@{viewModel.question.listOptions.get(0).toString()}"
            app:layout_constraintBottom_toTopOf="@+id/tvOption4"
            app:layout_constraintEnd_toStartOf="@+id/tvOption2"
            app:layout_constraintStart_toStartOf="parent"
            app:onOptionClickListener="@{(option) -> viewModel.chooseAnswer(option)}"
            tools:text="1" />

        <TextView
            android:id="@+id/tvOption2"
            style="@style/OptionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_orange_light"
            android:text="@{viewModel.question.listOptions.get(1).toString()}"
            app:layout_constraintBottom_toTopOf="@+id/tvOption5"
            app:layout_constraintEnd_toStartOf="@+id/tvOption3"
            app:layout_constraintStart_toEndOf="@+id/tvOption1"
            app:onOptionClickListener="@{(option) -> viewModel.chooseAnswer(option)}"
            tools:text="2" />

        <TextView
            android:id="@+id/tvOption3"
            style="@style/OptionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_purple"
            android:text="@{viewModel.question.listOptions.get(2).toString()}"
            app:layout_constraintBottom_toTopOf="@+id/tvOption6"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/tvOption2"
            app:onOptionClickListener="@{(option) -> viewModel.chooseAnswer(option)}"
            tools:text="3" />

        <TextView
            android:id="@+id/tvOption4"
            style="@style/OptionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_blue_bright"
            android:text="@{viewModel.question.listOptions.get(3).toString()}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/tvOption5"
            app:layout_constraintHorizontal_chainStyle="spread_inside"
            app:layout_constraintStart_toStartOf="parent"
            app:onOptionClickListener="@{(option) -> viewModel.chooseAnswer(option)}"
            tools:text="4" />

        <TextView
            android:id="@+id/tvOption5"
            style="@style/OptionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_red_light"
            android:text="@{viewModel.question.listOptions.get(4).toString()}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/tvOption6"
            app:layout_constraintStart_toEndOf="@+id/tvOption4"
            app:onOptionClickListener="@{(option) -> viewModel.chooseAnswer(option)}"
            tools:text="5" />

        <TextView
            android:id="@+id/tvOption6"
            style="@style/OptionTextStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_blue_light"
            android:text="@{viewModel.question.listOptions.get(5).toString()}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/tvOption5"
            app:onOptionClickListener="@{(option) -> viewModel.chooseAnswer(option)}"
            tools:text="6" />

        <TextView
            android:id="@+id/tvCountRightAnswers"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:text="@{viewModel.progressAnswers}"
            app:layout_constraintBottom_toTopOf="@+id/pbProgress"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvInvisibleNumber"
            app:setColorText="@{viewModel.enoughCountOfRightAnswers}"
            tools:text="@string/counter_right_answers" />

        <ProgressBar
            android:id="@+id/pbProgress"
            style="?android:progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            android:secondaryProgress="70"
            app:layout_constraintBottom_toTopOf="@+id/tvOption2"
            app:progress="@{viewModel.percentRightAnswers}"
            app:secondaryProgress="@{viewModel.minPercent}"
            app:setColorProgressBar="@{viewModel.enoughPercentOfRightAnswers}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>